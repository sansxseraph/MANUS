rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /projects/{userId}/{allPaths=**} {
      // Anyone can read project files
      allow read: if true;
      // Only the owner can upload project files
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
